server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # nacos集群换成nginx地址
        # server-addr: ip:1111

management:
  endpoints:
    web:
      exposure:
        include: "*"